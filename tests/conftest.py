"""Shared pytest fixtures for openrhyme-mcp.

Nothing here yet. Tests will run against a fixture SQLite store and a fake
``openrhyme`` binary on PATH so they never need the real engine or macOS permissions.
"""
